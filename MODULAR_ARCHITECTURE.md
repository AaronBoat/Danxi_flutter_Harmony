# 模块化架构 - 依赖关系说明

## 架构概览

```
┌─────────────────────────────────────────┐
│                Apps                     │
│  ┌─────────────────────────────────┐    │
│  │           app (壳工程)            │    │
│  │  • 基础配置                      │    │
│  │  • 路由管理                      │    │  
│  │  • 全局状态                      │    │
│  │  • 平台特定依赖                   │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
              ↓ 依赖
┌─────────────────────────────────────────┐
│               Modules                   │
│  ┌──────────┬──────────┬─────────────┐   │
│  │ home_    │dashboard_│ forum_      │   │
│  │ module   │ module   │ module      │   │
│  ├──────────┼──────────┼─────────────┤   │
│  │ course_  │timetable_│             │   │
│  │ module   │ module   │             │   │
│  └──────────┴──────────┴─────────────┘   │
└─────────────────────────────────────────┘
              ↓ 依赖
┌─────────────────────────────────────────┐
│               Common                    │
│  ┌──────────┬──────────┬─────────────┐   │
│  │ network  │  tools   │  widgets    │   │
│  │          │          │             │   │
│  └──────────┴──────────┴─────────────┘   │
└─────────────────────────────────────────┘
```

## 依赖解耦策略

### 1. 壳工程 (apps/app)
**职责**：
- 应用基础配置（主题、本地化、启动配置）
- 模块间路由和导航
- 全局状态管理
- 平台特定功能（如深度链接、窗口管理等）

**依赖原则**：
- ✅ 只依赖模块接口，不依赖具体实现
- ✅ 只保留无法下沉的平台特定依赖
- ❌ 不直接依赖业务相关的三方库

### 2. 业务模块 (modules/*)
**职责**：
- 封装特定业务领域的功能
- 管理业务相关的三方库依赖
- 提供统一的对外接口

**依赖原则**：
- ✅ 可以依赖公共模块 (common)
- ✅ 封装所有业务相关的三方库
- ❌ 模块间不能直接依赖

### 3. 公共模块 (common/*)
**职责**：
- 提供通用的基础功能
- 封装底层技术实现
- 为业务模块提供服务

**依赖原则**：
- ✅ 公共模块间可以有依赖关系 (tools → network → widgets)
- ✅ 封装底层技术细节
- ❌ 不能依赖业务模块

## 各模块的三方库依赖

### home_module
```yaml
dependencies:
  flutter_bloc: ^8.1.3      # 状态管理
  dio: ^5.4.3+1             # 网络请求  
  fluttertoast: ^8.2.6      # Toast提示
  shared_preferences: ^2.0.15 # 本地存储
  go_router: ^14.0.0        # 路由管理
```

### dashboard_module  
```yaml
dependencies:
  fl_chart: ^0.68.0         # 图表组件
  intl: ^0.19.0            # 国际化
  cached_network_image: ^3.3.1 # 图片缓存
  pull_to_refresh: ^2.0.0   # 下拉刷新
  permission_handler: ^11.3.1 # 权限管理
```

### forum_module
```yaml
dependencies:
  image_picker: ^1.0.0      # 图片选择
  flutter_markdown: ^0.6.18 # Markdown渲染
  photo_view: ^0.15.0       # 图片查看
  file_picker: ^8.0.0+1     # 文件选择
  infinite_scroll_pagination: ^4.0.0 # 无限滚动
```

### course_module
```yaml
dependencies:
  data_table_2: ^2.5.9      # 表格组件
  flutter_typeahead: ^5.2.0 # 搜索提示
  flutter_rating_bar: ^4.0.1 # 评分组件
  table_calendar: ^3.0.9    # 日历组件
  sqflite: ^2.3.0          # 本地数据库
```

## 鸿蒙适配策略

### 纯 Dart 库 (无需适配)
- flutter_bloc, dio, intl 等纯 Dart 实现的库
- 这些库在鸿蒙平台可以直接使用

### 平台依赖库 (需要适配)
- fluttertoast, image_picker, permission_handler 等
- 需要为鸿蒙平台提供相应的实现

### 适配步骤
1. **识别**: 在模块级别识别需要鸿蒙适配的库
2. **隔离**: 通过模块封装隔离平台差异
3. **适配**: 为鸿蒙平台提供对应实现
4. **切换**: 根据平台自动选择实现

## 开发和测试

### 模块独立开发
每个模块都可以独立开发和测试：
```bash
# 进入模块目录
cd modules/home_module

# 获取依赖
flutter pub get

# 运行测试
flutter test
```

### 壳工程集成测试
```bash
# 进入壳工程目录  
cd apps/app

# 获取所有依赖（包括模块依赖）
flutter pub get

# 运行应用
flutter run
```

## 模块接口设计原则

1. **明确的边界**: 每个模块都有清晰的功能边界
2. **统一的接口**: 通过导出文件提供统一的对外接口
3. **最小化依赖**: 只暴露必要的接口，隐藏实现细节
4. **版本兼容**: 保持接口的向后兼容性

这种架构的优势：
- ✅ **模块独立**: 每个模块可以独立开发、测试、部署
- ✅ **依赖隔离**: 三方库依赖被封装在模块内部
- ✅ **平台适配**: 便于为不同平台（如鸿蒙）提供适配
- ✅ **团队协作**: 不同团队可以并行开发不同模块
- ✅ **代码复用**: 公共模块可以在多个项目间复用
